syntax = "proto3";

message HolodeckEvent {
    uint64 timestamp = 1;
    oneof payload {
        bytes update = 2;
        string event = 3;
    }
}

message HolodeckStream {
    repeated HolodeckEvent events = 1;
}